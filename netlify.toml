[build]
  command = "npx @11ty/eleventy"
  publish = "_site"

[build.environment]
  NODE_VERSION = "18"

# Redirect /admin to the CMS
[[redirects]]
  from = "/admin/*"
  to = "/admin/:splat"
  status = 200
